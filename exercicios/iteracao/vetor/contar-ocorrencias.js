/**
 * Conta o total de ocorrências de um dado valor em um vetor.
 * 
 * @param {number[]} vetor Vetor de valores numéricos.
 * @param {number} valor O valor cujo total de ocorrência no vetor é desejado.
 * 
 * @returns A quantidade de ocorrências do valor no vetor fornecido.
 * Se não há ocorrência, então o retorno deve ser zero. Valores são
 * considerados idênticos, para efeito desta função, se a diferença dos
 * valores for inferior a 0.0001. 
 * 
 * @throws {TypeError} Gerado em vários cenários: (a) se o primeiro 
 * argumento não for um vetor; (b) se a função não é chamada exatamente
 * com dois argumentos; (c) se o vetor não contém apenas valores
 * numéricos; (d) se o segundo argumento não for um valor numérico.
 */
function contarOcorrencias(vetor, valor) {
}

module.exports = contarOcorrencias;